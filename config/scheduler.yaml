# =============================================================================
# 排程設定檔
# =============================================================================
# 此檔案定義所有資料收集的排程任務
# =============================================================================

# 全域設定
settings:
  # 預設維護作業 ID
  default_maintenance_id: "TEST-100"
  # 預設階段 (pre 或 post)
  default_phase: "post"

# 收集任務排程
jobs:
  # 光模組資料收集
  transceiver:
    indicator: transceiver
    interval: 300  # 每 5 分鐘收集一次
    enabled: true
    description: "光模組 Tx/Rx 功率資料收集"

  # 版本資訊收集
  version:
    indicator: version
    interval: 3600  # 每小時收集一次
    enabled: true
    description: "韌體版本資訊收集"

  # Uplink 連接狀態收集
  uplink:
    indicator: uplink
    interval: 600  # 每 10 分鐘收集一次
    enabled: true
    description: "Uplink 鄰居連接狀態收集"

  # 風扇狀態收集
  fan:
    indicator: fan
    interval: 600  # 每 10 分鐘收集一次
    enabled: true
    description: "設備風扇狀態收集"

  # 錯誤計數收集
  error_count:
    indicator: error_count
    interval: 300  # 每 5 分鐘收集一次
    enabled: true
    description: "Interface 錯誤計數收集"

  # 電源狀態收集
  power:
    indicator: power
    interval: 600  # 每 10 分鐘收集一次
    enabled: true
    description: "設備電源供應器狀態收集"

  # Ping 連通性收集
  ping:
    indicator: ping
    interval: 60  # 每 1 分鐘收集一次
    enabled: true
    description: "新設備 Ping 連通性檢查"

  # Port-Channel 狀態收集
  port_channel:
    indicator: port_channel
    interval: 300  # 每 5 分鐘收集一次
    enabled: true
    description: "Port-Channel 狀態及成員檢查"

  # ACL 設定收集 (預設停用)
  acl:
    indicator: acl
    interval: 1800  # 每 30 分鐘收集一次
    enabled: false
    description: "ACL 設定驗證"
