apiVersion: v1
kind: Service
metadata:
  name: network-dashboard
  namespace: network-dashboard
  labels:
    app: network-dashboard
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
  selector:
    app: network-dashboard
